# Rename from code to appropriate module name
require 'starter_code'

RSpec.describe StarterCode do
  it "has something of interest" do
    expect(false).to eq(true)
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
